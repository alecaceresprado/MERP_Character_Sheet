import { defineMessages } from 'react-intl';

export const scope = 'merp.HomePage';

export default defineMessages({
  viewButton: {
    id: `${scope}.view-button`,
    defaultMessage: 'View'
  },
  editButton: {
    id: `${scope}.edit-button`,
    defaultMessage: 'Edit'
  }
});
